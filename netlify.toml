[build]
base = "nb-tennis"
command = "corepack enable && pnpm install --no-frozen-lockfile && pnpm build"
publish = "dist"

[build.environment]
NODE_VERSION = "22"
NETLIFY_USE_PNPM = "true"
PNPM_FLAGS = "--no-frozen-lockfile"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
