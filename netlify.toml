[build]
  command = "rm -f pnpm-lock.yaml && corepack enable && pnpm install --no-frozen-lockfile --prod=false && pnpm --filter @workspace/nb-tennis build"
  publish = "nb-tennis/dist"

[build.environment]
  NODE_VERSION = "22"
