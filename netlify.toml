[build]
  command = "corepack enable && pnpm install --no-frozen-lockfile --prod=false && cd nb-tennis && pnpm build"
  publish = "nb-tennis/dist"

[build.environment]
  PNPM_FLAGS = "--no-frozen-lockfile --prod=false"
