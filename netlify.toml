[build]
  command = "corepack enable && pnpm install --no-frozen-lockfile --prod=false && pnpm --filter @workspace/nb-tennis build"
  publish = "nb-tennis/dist"

[build.environment]
  PNPM_FLAGS = "--no-frozen-lockfile --prod=false"
